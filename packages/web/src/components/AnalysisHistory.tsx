import { Container, Header, Box } from '@cloudscape-design/components';

export default function AnalysisHistory() {
  return (
    <Container header={<Header>Analysis History</Header>}>
      <Box>No analysis history available</Box>
    </Container>
  );
}
